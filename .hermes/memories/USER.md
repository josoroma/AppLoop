User is building AppLoop, a Next.js visual app builder. Key preferences: (1) No automatic screenshots on inspect click — only manual paste (Ctrl+V) via clipboard. (2) Multi-select in inspect mode with toggle behavior — clicking an element adds/removes it, checkboxes above prompt, cleared on send. (3) Every repeated element needs a shared base classname AND a unique per-instance classname (e.g. `metric-card summary-card metric-revenue`). (4) Templates must have unique root classnames (`template-default`, `template-admin-luma`) for disambiguation. (5) Targets cleared when prompt is sent and when runtime stops. (6) Chat conversation area and targets area must be scrollable with textarea always visible. (7) Generated projects need CHOKIDAR_USEPOLLING to detect file changes from Hermes prompts.